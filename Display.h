#pragma once
#include <SFML\Graphics.hpp>
using namespace std;

class Display{

public:
    Display();
    //void draw(RenderWindow& window);
    //void jugar(RenderWindow& window, string& name);
    ~Display();
};
